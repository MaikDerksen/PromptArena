# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1

# Environment variables accessible to your application at runtime.
# For secrets, it's best practice to use Google Cloud Secret Manager.
environmentVariables:
  # Replace with your actual production domain
  NEXT_PUBLIC_APP_URL: "https://promptarena-aed1c.web.app" # Or your custom domain if you set one up
  # Replace with your actual LIVE Stripe Publishable Key
  NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: "pk_live_YOUR_STRIPE_LIVE_PUBLISHABLE_KEY_HERE"

# Secret environment variables fetched from Google Cloud Secret Manager.
# You need to create these secrets in Secret Manager first and grant
# the App Hosting service agent the "Secret Manager Secret Accessor" role.
secretEnvironmentVariables:
  - key: STRIPE_SECRET_KEY # The env var name your app uses (process.env.STRIPE_SECRET_KEY)
    secret: stripe-live-secret-key # The ID of the secret in Secret Manager (e.g., the name you gave it)
    # Optional: specify version, defaults to 'latest'
    # version: "1" 
  - key: STRIPE_WEBHOOK_SECRET
    secret: stripe-live-webhook-secret # The ID of the secret in Secret Manager
  - key: GOOGLE_API_KEY
    secret: google-ai-api-key # The ID of the secret in Secret Manager
