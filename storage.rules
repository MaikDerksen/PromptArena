
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // A user can read/write files in their own user directory.
    match /user-images/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
